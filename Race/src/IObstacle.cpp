#include "IObstacle.h"

IObstacle::IObstacle(word start, word end, uint32_t color) : _start(start),
                                                             _end(end),
                                                             _color(color)
{
}